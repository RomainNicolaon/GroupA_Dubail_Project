import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dubaii.com',
  appName: 'DubaiI',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
